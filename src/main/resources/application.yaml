spring:
  datasource:
    url: jdbc:mysql://localhost:3306/studay
    username: root
    password: root1234!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: debug